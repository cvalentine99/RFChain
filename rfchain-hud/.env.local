# RFChain HUD - Production Configuration
# Generated: 2026-01-04
# IMPORTANT: This app requires MySQL/MariaDB - SQLite is NOT supported

# =============================================================================
# DATABASE - MySQL/MariaDB REQUIRED (SQLite NOT supported)
# =============================================================================
# Option 1: Local MySQL
# DATABASE_URL="mysql://user:password@localhost:3306/rfchain_hud"
#
# Option 2: Remote MySQL (e.g., PlanetScale, TiDB)
# DATABASE_URL="mysql://user:password@host:port/database?ssl=true"
#
# For local development, install MySQL:
#   sudo apt install mysql-server
#   sudo mysql -e "CREATE DATABASE rfchain_hud; CREATE USER 'rfchain'@'localhost' IDENTIFIED BY 'yourpassword'; GRANT ALL ON rfchain_hud.* TO 'rfchain'@'localhost';"
DATABASE_URL="mysql://rfchain:yourpassword@localhost:3306/rfchain_hud"

# =============================================================================
# SERVER
# =============================================================================
PORT=3007
NODE_ENV=production
HOST=0.0.0.0

# JWT Secret for session tokens (generate with: openssl rand -base64 32)
JWT_SECRET="j1MwBxxKzcni9vojU02Dj9aFMn3yLcs0irv+3rt/hy4="

# =============================================================================
# PYTHON CONFIGURATION - For signal analysis
# =============================================================================
# Path to Python interpreter (use conda env python for GPU support)
# Find with: conda activate rfchain-hud && which python
PYTHON_PATH=/home/cvalentine/miniconda3/envs/rfchain-hud/bin/python

# Path to the analysis script
ANALYSIS_SCRIPT_PATH=/home/cvalentine/RFChain/analyze_signal_v2.2.2_forensic.py

# Output directories
ANALYSIS_OUTPUT_DIR=/home/cvalentine/RFChain/rfchain-hud/analysis_output
UPLOAD_DIR=/home/cvalentine/RFChain/rfchain-hud/uploads

# =============================================================================
# GPU - RTX 4090 Configuration
# =============================================================================
GPU_ENABLED=true
CUDA_VISIBLE_DEVICES=0
GPU_MEMORY_FRACTION=0.8
CUPY_ACCELERATORS=cub

# =============================================================================
# AUTHENTICATION (Optional - for Manus OAuth)
# Leave commented for local-only deployment without login
# =============================================================================
# VITE_APP_ID=your-manus-app-id
# OAUTH_SERVER_URL=https://api.manus.im
# VITE_OAUTH_PORTAL_URL=https://manus.im/login

# =============================================================================
# STORAGE (Optional - for cloud file storage)
# Leave commented for local-only file storage
# =============================================================================
# BUILT_IN_FORGE_API_URL=https://api.manus.im/forge
# BUILT_IN_FORGE_API_KEY=your-forge-api-key
# VITE_FRONTEND_FORGE_API_URL=https://api.manus.im/forge
# VITE_FRONTEND_FORGE_API_KEY=your-frontend-forge-key

# =============================================================================
# AI ASSISTANT (Optional - for JARVIS chat)
# =============================================================================
# ANTHROPIC_API_KEY=sk-ant-your-key-here
