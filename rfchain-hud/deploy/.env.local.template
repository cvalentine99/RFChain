# RFChain HUD - Local Deployment Configuration
# Copy this file to .env.local and configure for your environment

# Server Configuration
PORT=3007
NODE_ENV=production
HOST=0.0.0.0

# Database Configuration (SQLite for local deployment)
DATABASE_URL=file:./data/rfchain.db

# JWT Secret (generate a secure random string)
# Use: openssl rand -base64 32
JWT_SECRET=your-secure-jwt-secret-here

# Python Configuration
PYTHON_PATH=/home/your-username/miniconda3/envs/rfchain-hud/bin/python
ANALYSIS_SCRIPT_PATH=/home/your-username/RFChain/analyze_signal_v2.2.2_forensic.py
ENHANCED_MODULE_PATH=/home/your-username/RFChain/enhanced_analysis_module.py

# File Storage Paths (local storage)
UPLOAD_DIR=./uploads
ANALYSIS_OUTPUT_DIR=./analysis_output
AUDIO_DIR=./audio

# LLM Configuration (choose one)
# Option 1: Anthropic API
ANTHROPIC_API_KEY=your-anthropic-api-key

# Option 2: OpenAI API
# OPENAI_API_KEY=your-openai-api-key

# Option 3: Local LLM (Ollama)
# LOCAL_LLM_URL=http://localhost:11434
# LOCAL_LLM_MODEL=llama3.2

# Whisper API for Voice Input (optional)
# WHISPER_API_KEY=your-whisper-api-key

# RAG Configuration
RAG_ENABLED=true
RAG_SIMILARITY_THRESHOLD=0.7
RAG_MAX_RESULTS=5

# =============================================================================
# GPU ACCELERATION (NVIDIA CUDA)
# =============================================================================
# Enable GPU acceleration for signal analysis (requires NVIDIA GPU + CUDA)
GPU_ENABLED=true

# CUDA device to use (0 = first GPU, 1 = second GPU, etc.)
CUDA_VISIBLE_DEVICES=0

# CuPy memory pool settings (improves performance for repeated operations)
# Options: default, managed, unified
CUPY_ACCELERATORS=cub,cutensor

# GPU memory limit (fraction of total GPU memory, 0.0-1.0)
# Set lower if running other GPU applications simultaneously
GPU_MEMORY_FRACTION=0.8

# =============================================================================
# LOGGING
# =============================================================================
LOG_LEVEL=info
LOG_FILE=./logs/rfchain-hud.log
