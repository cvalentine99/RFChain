# RFChain HUD - Local Environment Configuration
# Copy this file to .env.local and fill in your values
# cp .env.local.example ../.env.local

# =============================================================================
# DATABASE CONFIGURATION (Required)
# =============================================================================
# For local SQLite (simplest option):
DATABASE_URL="file:./data/rfchain.db"

# For MySQL/MariaDB:
# DATABASE_URL="mysql://user:password@localhost:3306/rfchain_hud"

# For TiDB Cloud (if using cloud database):
# DATABASE_URL="mysql://user:password@gateway.tidbcloud.com:4000/rfchain_hud?ssl=true"

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
PORT=3007
NODE_ENV=production
HOST=0.0.0.0

# JWT Secret for session tokens (generate with: openssl rand -base64 32)
JWT_SECRET="your-secure-random-secret-here-change-this"

# =============================================================================
# GPU CONFIGURATION (Optional - for RTX 4090)
# =============================================================================
GPU_ENABLED=true
CUDA_VISIBLE_DEVICES=0
GPU_MEMORY_FRACTION=0.8
CUPY_ACCELERATORS=cub

# =============================================================================
# ANALYSIS CONFIGURATION
# =============================================================================
# Path to Python analysis scripts
ANALYSIS_SCRIPT_PATH=/home/cvalentine/RFChain/analyze_signal_v2.2.2_forensic.py

# Output directory for analysis results
ANALYSIS_OUTPUT_DIR=/opt/rfchain-hud/analysis_output

# Upload directory for signal files
UPLOAD_DIR=/opt/rfchain-hud/uploads

# =============================================================================
# OPTIONAL: External Services
# =============================================================================
# Anthropic API (for JARVIS AI assistant)
# ANTHROPIC_API_KEY=sk-ant-...

# S3 Storage (for cloud file storage)
# S3_BUCKET=your-bucket-name
# S3_REGION=us-east-1
# S3_ACCESS_KEY=...
# S3_SECRET_KEY=...

# =============================================================================
# OPTIONAL: OAuth (if using Manus authentication)
# =============================================================================
# VITE_APP_ID=your-app-id
# OAUTH_SERVER_URL=https://api.manus.im
# VITE_OAUTH_PORTAL_URL=https://manus.im/login
